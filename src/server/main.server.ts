import { Players, TweenService } from "@rbxts/services";
import Weave from "shared/Weave";

// const players = Weave.WeaveObject<Record<string, boolean>>("PlayerLeves", {});

// Players.PlayerAdded.Connect((player: Player) => {
// 	const p = players.get();
// 	p[`${player.UserId}`] = true;
// 	players.set(p);
// });

print("here i amasdf server");
